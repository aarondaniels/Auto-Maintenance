services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: amt
      POSTGRES_PASSWORD: amt
      POSTGRES_DB: amt
    ports:
      - "5432:5432"
    volumes:
      - amt_pgdata:/var/lib/postgresql/data

volumes:
  amt_pgdata:
